<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="install" xml:lang="en-GB">
  <title>Installing the plugin</title>
  <shortdesc>Install the plugin into a DITA Open Toolkit with a single command; there are no
    other dependencies.</shortdesc>
  <taskbody>
    <prereq>
      <p>You need the <xref keyref="dita-ot-site"/> version 4.4.1 or later, running on Java
        17 or later. No Node.js, no package managers, and no network access at build time.</p>
    </prereq>
    <steps>
      <step>
        <cmd>Install the plugin from the latest release.</cmd>
        <info>
          <codeblock>dita install https://github.com/iStandUK/govuk-dita-plugin/releases/download/v0.9.0/org.istanduk.gov-uk-0.9.0.zip</codeblock>
        </info>
      </step>
      <step>
        <cmd>Confirm the <codeph>govuk</codeph> format is available.</cmd>
        <info>
          <codeblock>dita transtypes</codeblock>
          <p>The list should include <codeph>govuk</codeph>.</p>
        </info>
      </step>
    </steps>
    <result>
      <p>The toolkit can now build any DITA map with <codeph>--format=govuk</codeph>.</p>
      <note type="important">To upgrade to a newer release, uninstall first — the toolkit
        refuses to replace an installed plugin and leaves the old version in place:</note>
      <codeblock>dita uninstall org.istanduk.gov-uk
dita install &lt;newer release URL&gt;</codeblock>
    </result>
  </taskbody>
</task>
