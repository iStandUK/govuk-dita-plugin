<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="branding" xml:lang="en-GB">
  <title>Branding and the legal position</title>
  <shortdesc>By default the output uses GOV.UK Design System components and layout without
    any government branding, because the crown and the GDS Transport typeface are legally
    restricted.</shortdesc>
  <conbody>
    <note type="warning">The GOV.UK crown, crest, and the GDS Transport typeface may only be
      used by official GOV.UK services. Publishing with them from any other organisation
      misrepresents your site as government-run.</note>
    <section>
      <title>Neutral mode (the default)</title>
      <p><indexterm>branding<indexterm>neutral mode</indexterm><indexterm>official mode</indexterm></indexterm><indexterm>fonts</indexterm>Neutral builds ship no restricted assets at all, and the stylesheet is overlaid so
        that none is ever requested by a browser:</p>
      <ul>
        <li>system fonts stand in for GDS Transport</li>
        <li>a plain masthead carries your service name instead of the crown</li>
        <li>the footer carries no crest and no crown-copyright or Open Government Licence
          statement — instead it shows <i>your</i> publication's copyright and attribution,
          drawn from the bookmap's <codeph>bookmeta</codeph> (see below)</li>
      </ul>
      <p>This makes neutral mode safe for standards bodies, local government partnerships,
        and any publisher who wants the Design System's clarity without its insignia.</p>
    </section>
    <section>
      <title>The iStandUK theme</title>
      <p>Setting <codeph>govuk.branding=istanduk</codeph> layers the iStandUK brand on top
        of neutral mode: the header becomes a white bar carrying the iStandUK logo above a
        brand-blue rule, and the footer band takes the brand blue (Pantone 072). The logo
        artwork ships as outlined vector paths, so no fonts are required or distributed, and
        every neutral-mode guarantee still holds.</p>
      <fig outputclass="app-screenshot">
        <title>The iStandUK theme</title>
        <image href="../images/branding-istanduk.png">
          <alt>Home page with a white header carrying the iStandUK logo and publication
            title, a search field, and a dark blue rule beneath the header.</alt>
        </image>
      </fig>
    </section>
    <section>
      <title>Footer credits from the bookmap</title>
      <p>Whatever the branding, the footer credits the publication itself, drawn from the
        bookmap's <codeph>bookmeta</codeph>:</p>
      <ul>
        <li>a copyright line — the <codeph>©</codeph> symbol, the years from
          <codeph>copyrfirst</codeph>/<codeph>copyrlast</codeph>, and the
          <codeph>bookowner</codeph> — for example <q>© 2026 iStandUK</q>;</li>
        <li>an attribution line naming the <codeph>author</codeph> or
          <codeph>publisherinformation</codeph> when it differs from the owner.</li>
      </ul>
      <p>The same credit appears on every page — content, home, glossary and the rest —
        so it travels with each page a reader might land on or print. A map with no
        <codeph>bookmeta</codeph> simply shows its title instead.</p>
    </section>
    <section>
      <title>Official mode (not yet implemented)</title>
      <p>Setting <codeph>govuk.branding=official</codeph> is reserved for genuine GOV.UK
        services. The build currently prints a warning and continues with neutral assets;
        when implemented, official mode will source fonts and crown imagery from the
        publisher's own copy of govuk-frontend rather than from this plugin, which never
        redistributes them.</p>
    </section>
  </conbody>
</concept>
